# Release: v1 (full integrated build)

**Deliverable:** v1 — full integrated build covering all 4 area deliverables + integration agents
**Status:** done — v1 signoff **granted** (all caveats closed; follow-up bundles A/B/M/P shipped)
**Source plan reference:** `docs/designs/temporal-knowledge-graph.md` (locked v1 scope)
**Released:** 2026-05-06; caveats closed + Tier A/B/M/P bundles landed 2026-05-06
**v1 parent-task review:** T-INT-04 (`pass_with_findings` → originally `granted_with_caveats`; caveats now closed → `granted`)

---

## Summary

Full v1 build of `tm` (time-manager) — a personal process-mining + outcome-scoring tool. The four headline deliverables ship as a coherent integrated product: a transcript flows through the debrief agent → events → process mining → variant clustering → outcome scoring → scheduler suggestion → telemetry, end to end, with no integration gaps. The build comprises ~53 subtasks shipped across 19 dispatch waves, 711 tests, 11 schema migrations, and 4 area release docs. Test suite is fully green (711 passed + 1 skipped); mypy clean across 58 source files; ruff check clean; ruff format clean across 117 files. **The four headline deliverables are production-grade. The three originally critical caveats, the audit-surfaced operator-surface gap (no `tm debrief` / `tm suggest` CLI), and the multi-backend LLM routing gap (`TM_LLM_BACKEND` documented but unread in code) are all closed.**

---

## Sub-deliverable release docs

This document is the v1-level canonical record. Each headline area has its own deeper release doc:

| Area | Status | Release doc | Cluster commit anchor |
|---|---|---|---|
| **Vocabulary governance** | shipped | [docs/release/vocabulary.md](./vocabulary.md) | `0348379` |
| **Goals (first-class)** | shipped | [docs/release/goals.md](./goals.md) | `ca31a2b` |
| **Process mining** | shipped | [docs/release/process_mining.md](./process_mining.md) | `d8062ff` |
| **Outcome scoring** | shipped | [docs/release/outcome_scoring.md](./outcome_scoring.md) | `e188119` |

---

## Public surface (aggregate)

### CLI commands (16 entries under `tm`: 15 sub-typers plus `init`)

- `tm init` — bootstrap: applies migrations + seeds starter vocabulary/aliases (closed Caveat #1, commit `0541fc3`)
- `tm backup [--output PATH] [--overwrite]`
- `tm bottlenecks [--lens] [--since] [--until]`
- `tm capture telegram|calendar|voice` — local Telegram JSON, `.ics`, and already-transcribed voice-note ingestion
- `tm daemon start/stop/status`
- `tm dashboard`
- `tm debrief [--transcript-file PATH | --from-stdin] [--case-date YYYY-MM-DD] [--monthly-cap-usd N]` — operator surface for DebriefAgent (commit `c1674fd`)
- `tm discover [--lens workday|goal_pursuit] [--since] [--until]`
- `tm encryption status|set-key`
- `tm export [--output PATH]`
- `tm goal add/list/complete/abandon/show`
- `tm privacy redact|forget`
- `tm reextract --case-date YYYY-MM-DD [--transcript-file PATH]`
- `tm suggest [--case-date YYYY-MM-DD] [--case-goal-id ULID] [--monthly-cap-usd N]` — operator surface for SchedulerAgent (commit `c1674fd`)
- `tm variants [--lens] [--since] [--until] [--top-n] [--trend]`
- `tm vocab review/list/drift` — `drift` surfaces drifted canonicals + novelty rate (commit `2558fae`)

### Daemon RPC handlers (16)

`ping`, `append_event`, `add_goal`, `complete_goal`, `abandon_goal`, `add_alias`, `add_canonical`, `archive_vocabulary`, `log_suggestion`, `record_actual_outcome`, `record_thumbs`, `check_budget`, `record_cost`, `rebuild_kuzu_projection` (commit `d234e26`), `run_debrief` (commit `f2b39b1`), `propose_suggestion` (commit `f2b39b1`).

### Importable APIs

- **Models:** `tm.models.goals.{Goal, ulid, validate_priority}`, `tm.models.outcome.{compute_outcome_score, compute_outcome_breakdown, OutcomeBreakdown, OutcomeAggregator, DEFAULT_TASK_COMPLETION_THRESHOLD}`
- **Repositories:** `tm.repositories.{goals.GoalsRepository, vocabulary.VocabularyRepository, events.EventsRepository, telemetry.SuggestionTelemetryRepository, transcripts.TranscriptRepository}`
- **Engines:** `tm.engines.process_mining.{ProcessMiner, DiscoveredModel, ConformanceResult, Variant, VariantAnalysis, PerformanceMetric, PerformanceAnalysis}`, `tm.engines.variant_cluster.{VariantClusterer, LabeledVariant, VariantClustering}`, `tm.engines.scheduler_metric.{SchedulerSuccessMetric, SchedulerMetricSummary}`, `tm.engines.prescriptive_monitoring.{Guardrails, ObjectiveFunctionGuard, CounterfactualGuard, ConformanceDeviationGuard, CandidateSuggestion, GuardrailVerdict, GuardrailsEvaluation}`
- **Stores:** `tm.stores.sqlite_store.{SQLiteStore, MigrationError, MigrationPragmaError, MigrationPostTxnError, MigrationBodyError, MigrationIntegrityError}`, `tm.stores.kuzu_store.{KuzuStore, PersistedModel}`, `tm.stores.kuzu_projection.{project_discovered_model_to_kuzu, rebuild_kuzu_projection, compute_model_id}`
- **Petri-net data shapes:** `tm.engines.petri_net.{PlaceData, TransitionData, ArcData, MarkingData, PetriNetData, petri_net_data_from_pm4py, petri_net_data_to_pm4py}` (round-trip pm4py ↔ frozen-dataclass converters; canonical home as of commit `09cca89`)
- **LLM:** `tm.llm.client.{LLMClient (Protocol), Message, ChatResponse, ToolCall, ToolCallResponse, ExtractResponse, Usage}`, `tm.llm.anthropic_adapter.AnthropicAdapter`, `tm.llm.codex_adapter.CodexAdapter` (subprocess to `codex exec --json`, commit `ecb8212`), `tm.llm.claude_code_adapter.ClaudeCodeAdapter` (subprocess to `claude --bare --print`, commit `8aad192`), `tm.llm.factory.{build_llm_client, BACKEND_ENV, VALID_BACKENDS}` (TM_LLM_BACKEND selector, commit `6755760`), `tm.llm.cost_meter.CostMeter`, `tm.llm.errors.{LLMClientError, CostCapExceeded}`
- **Agents:** `tm.agents.debrief.{DebriefAgent, DebriefResult, ExtractedEvent, EXTRACTOR_VERSION}`, `tm.agents.scheduler.{SchedulerAgent, ScheduledSuggestion, SchedulerSkipReason, SCHEDULER_VERSION}`
- **Daemon:** `tm.daemon.{TMDaemon, DaemonClient, DaemonRequest, DaemonResponse}`
- **Vocab alignment:** `tm.vocab_alignment.{VocabAligner, AlignmentResult, lower_normalize}`

### Schema migrations (11)

| # | File | Adds |
|---|---|---|
| 0001 | `events + schema_migrations` | base XES events + migration bookkeeping |
| 0002 | `cost_ledger` | LLM cost tracking |
| 0003 | `goals` | first-class goals table |
| 0004 | `vocabulary + aliases` | 16-activity starter set + 5 seed aliases |
| 0005 | `events.advances_goal` | soft-FK column + partial index |
| 0006 | `events XES columns` | `case_date`, `case_goal_id`, `vocab_version`, `schema_version` |
| 0007 | `suggestion_telemetry` | scheduler suggestion log |
| 0008 | `goals_name_check` | rebuild adding `CHECK (length(trim(name)) > 0)` |
| 0009 | `goals_name_check_tighten` | rebuild adding tab/newline-aware whitespace check |
| 0010 | `debrief_summary_uniqueness` | pre-cleans duplicates + partial UNIQUE index on `events(case_date) WHERE activity='debrief_summary'`; closes codex-flagged concurrency race after LLM handlers were lifted off `self._lock` |
| 0011 | `capture_transcripts_privacy` | transcript retention for reextract plus privacy action audit rows |

### Runtime dependencies

- `typer >= 0.12` (CLI)
- `anthropic >= 0.40, < 1` (LLM)
- `pm4py >= 2.7, < 3` (process mining; pulls pandas/numpy/scipy/networkx, ~50MB transitive)
- `kuzu >= 0.11, < 1` (graph DB for persisted Petri nets, ~25MB)

---

## Tests added

**711 passed + 1 skipped + 0 failed.** mypy clean (58 source files). ruff check clean. ruff format clean (117 files).

Coverage layered:
- Unit tests across each module (model invariants, repo CRUD, engine math, agent pipelines)
- Integration coverage at `tests/integration/test_full_pipeline.py` exercises the full chain end-to-end (real repos + real engines + mocked LLM at the boundary)
- Eval-harness coverage at `tests/integration/test_eval_harness.py` includes `live_llm=True` harness mode (commit `f41a830`; live smoke gated on `TM_LLM_API_KEY`) plus synthetic transcript fixtures asserting plan thresholds (field ≥0.85, trace ≥0.70, variant ≥0.75) — all met at 1.0 baseline
- 6 reverse-converter unit tests at `tests/engines/test_petri_net.py` (commit `2659e64`)

---

## Key decisions and rationale (v1-level)

1. **Locked outcome formula** — `int(did_complete_tasks) + int(did_advance_goal) ∈ {0, 1, 2}` is hardcoded across the chain (not user-configurable in v1). Lets downstream LLM rationales quote the formula verbatim without retraining.
2. **Two named case lenses** (`workday` keyed on `case_date`; `goal_pursuit` keyed on `case_goal_id`) — explicit, type-checked entry points instead of free-form case keys.
3. **Frozen dataclasses on every public engine return** — keeps PM4Py/Kuzu/Pydantic types from leaking across module boundaries; makes everything serializable + immutable.
4. **Plain dataclasses + manual validation, no Pydantic** — keeps the dependency surface narrow. Anthropic SDK transitively pulls Pydantic v2 but we don't depend on it explicitly.
5. **Single-writer daemon scaffold** — `tm/daemon.py` enforces the write path via Unix-socket for any future multi-writer scenario (Telegram bot, etc.). v1 agents bypass the daemon for simplicity (see ARCH-2 in Risks).
6. **Per-call sqlite3 connections in repositories** — avoids long-lived connection state; lets tests pass `tmp_path` DBs without fixture-managed Stores.
7. **Migration runner with `-- !pre-txn:` / `-- !post-txn:` pragma directives** (T-FND-07) — supports table rebuilds (e.g. CHECK constraints, FK additions) that need `PRAGMA foreign_keys=OFF` outside the transaction.
8. **Migration exception hierarchy:** `MigrationError` → `MigrationPragmaError` / `MigrationPostTxnError` / `MigrationBodyError` / `MigrationIntegrityError` (T-FND-08, T-FND-09). `except MigrationError:` catches every runner failure mode.
9. **Vocabulary case-symmetry** — `add_alias` lowercases + strips inputs; `resolve` symmetrically lowercases lookups (T-VOC-01-polish closed the case-asymmetry bug surfaced by T-VOC-04).
10. **PrPM guardrails** (T-INT-02) — three independent guards (Objective: strict `>0`; Counterfactual: `≥0.3`; Conformance: `≥0.4`) run unconditionally with no short-circuit so rationale captures every reason.
11. **Single-summary-per-case_date contract** (T-INT-01) — DebriefAgent enforces at most one summary event with `activity='debrief_summary'` per case_date. Required for OutcomeAggregator's per-attribute MAX semantics to compute correctly.
12. **ProcessMiner filters `debrief_summary` by default** (T-INT-01b) — opt-in via `include_summary_events=True`. Prevents the synthetic summary from polluting variant analysis.

---

## Critical Caveats — all CLOSED

1. ~~**`tm init` is a stub.**~~ **CLOSED** in commit `0541fc3` — `tm init` applies migrations + seeds starter vocabulary/aliases idempotently.
2. ~~**README is two lines.**~~ **CLOSED** in commit `385030d` — full quickstart, env-var reference, command index, architecture overview.
3. ~~**AnthropicAdapter.extract drops the SDK's usage block.**~~ **CLOSED** in commit `5869b8c` — `LLMClient.extract` Protocol now returns `ExtractResponse(data, usage)`; cost ledger reports input + output tokens correctly.

### Audit-surfaced operator-surface gap — also CLOSED

After Tier A polish landed, an audit found that the v1 deliverables were complete but lacked end-to-end CLI exposure: DebriefAgent and SchedulerAgent had no operator surface, so non-developers couldn't run a debrief or get a suggestion from the CLI. **Closed in commit `c1674fd`** — `tm debrief` + `tm suggest` CLIs ship with 10 dedicated tests; helper extraction commit `8a063fc` consolidated shared CLI scaffolding into `tm/commands/_shared.py`.

---

## Architectural risks (acknowledged; tracked)

| ID | Severity | Risk |
|---|---|---|
| ~~ARCH-1~~ | ~~high~~ | **RESOLVED** (commit `5869b8c`) — `LLMClient.extract` returns `ExtractResponse` with structured `Usage`; cost ledger reports both input and output tokens. |
| ARCH-2 | medium | Single-writer daemon enforced by convention only — agents bypass via direct repo injection. Fine for v1 single-user CLI; revisit when daemon becomes mandatory. **Update (commit `350a2b1`):** `run_debrief` + `propose_suggestion` handlers now bypass `self._lock` (per-method dispatch classification). **Update (commit `f17ce3f`):** the resulting race condition (codex /codex review caught it: two concurrent `run_debrief` for the same `case_date` could both pass DebriefAgent's check-then-act and INSERT duplicate summaries) is now closed at the DB layer via migration 0010's partial UNIQUE index. DebriefAgent translates the resulting `sqlite3.IntegrityError` into `DuplicateSummaryError`; CLI renders friendly message; daemon envelope surfaces `error="DuplicateSummary"` with `case_date`. |
| ARCH-3 | medium | `events.advances_goal` soft-FK has TOCTOU between SELECT and INSERT. Closes when events table rebuild lands the formal `REFERENCES` (T-PM-02 follow-up) |
| ~~ARCH-4~~ | ~~medium~~ | **RESOLVED** (commit `f17ce3f`) — migration 0010 adds a partial UNIQUE index for `debrief_summary` per `case_date`, so the one-summary-event contract is enforced at the database write boundary. |
| ARCH-5 | low | Magic strings shared by reference across modules (`workday`/`goal_pursuit` lens names, cluster labels). Renames require grep-wide sweeps. Labels are LLM-quotable per the locked-formula decision |
| ARCH-6 | low | Three repository write paths overlap (tm/store.py, tm/stores/sqlite_store.py, tm/repositories/events.py). Acknowledged |
| ~~ARCH-7~~ | ~~low~~ | **RESOLVED** (commit `09cca89`) — `PetriNetData` is now first-class on `DiscoveredModel`; `kuzu_projection` consumes it directly, no re-mine, `# noqa: SLF001` removed. |
| ARCH-8 | low | `EventsRepository.query_events` `until` is exclusive but `list_distinct_case_dates` `until` is inclusive — asymmetric footgun. **Module-level invariant note added in commit `147b138`.** |

---

## Migration / upgrade notes

First time landing — no breaking changes. Existing dev databases pick up all 11 migrations idempotently on next CLI invocation; `schema_migrations` records sha256 checksums and refuses to re-apply on file mutation.

Cold install: ~30-60s pip install due to `pm4py` + `kuzu` transitive footprint (~75MB total).

---

## Outstanding follow-ups inventory

### High priority (close before first user) — ALL CLOSED
- ~~**T-INT-04-init**~~ — **DONE** (commit `0541fc3`)
- ~~**T-INT-04-readme**~~ — **DONE** (commit `385030d`)
- ~~**T-INT-04-llm-extract-usage**~~ — **DONE** (commit `5869b8c`)

### Medium priority — ALL CLOSED
- ~~**T-VOC-01c**~~ — DONE (case-asymmetry closed in T-VOC-01-polish bundle, commit `12c463f`).
- ~~**T-PM-02-followup-b**~~ — **DONE** (commit `2659e64`) — conformance now rehydrates from `model.petri_net` via the new `petri_net_data_to_pm4py` reverse converter; falls back to re-mine only when `petri_net is None`. `rehydration_source` field in `extractor_metadata` exposes which path was taken.
- ~~**T-INT-02-daemon-wiring**~~ — **DONE** — `rebuild_kuzu_projection` exposed as RPC handler (commit `d234e26`); `run_debrief` + `propose_suggestion` handlers added (commit `f2b39b1`). External cron/systemd timer can now drive automation via the daemon socket.

### Low priority polish — ALL CLOSED
- ~~**T-INT-02b**~~ — DONE (commit `543d223`).
- ~~**T-PM-02-followup-a**~~ — DONE (commit `09cca89`) — `PetriNetData` first-class on `DiscoveredModel`.
- ~~**T-VOC-01b**~~ — DONE (commit `12c463f`).
- ~~**T-VOC-01d**~~ — closed during Tier A polish bundle.
- ~~**T-VOC-03b**~~ — DONE (commit `d51fbf6`) — m-branch abort, c-branch failure counter, auto-seed on `tm vocab list`, weak test disjunct tightened.
- ~~**T-INT-04-events-until-doc**~~ — DONE (commit `147b138`).
- ~~**T-OPS-cost-flag** + **T-OPS-cli-helpers**~~ — DONE (commit `8a063fc`) — `--cost-cap-usd` renamed to `--monthly-cap-usd`; shared CLI helpers extracted to `tm/commands/_shared.py`.

### Recently closed in v1.1 local-operator branch
- **Daemon proper daemonization.** `tm daemon start --no-foreground` now performs POSIX double-fork detach with stdio redirected to `/dev/null`; foreground remains default for process supervisors.
- **Variant trend labeling.** `tm variants --trend --since ... --until ...` now compares the requested window to the previous equal-size window and labels current variants as `new`, `rising`, `falling`, or `stable`.
- **Live-LLM eval threshold split.** `live_llm=True` now uses separate live thresholds (field ≥0.60 / trace ≥0.20 / variant ≥0.50). No fresh live run was possible in this branch without `TM_LLM_API_KEY`; the companion eval repo should recalibrate with real examples.
- ~~**Concurrency carry-forward (ARCH-2 follow-up).** `run_debrief` and `propose_suggestion` handlers hold `self._lock` for the duration of the LLM call.~~ **CLOSED.** Per-method dispatch classification (commit `350a2b1`) lifted the lock from LLM handlers; the resulting same-case_date race surfaced by codex review is closed by migration 0010 + `DuplicateSummaryError` plumbing (commit `f17ce3f`).

### Deferred (v1.1 / v2 backlog — see TODOS.md)
- **Subprocess-adapter shared helpers.** Before adding a 4th LLM adapter (OllamaAdapter / OpenAIAdapter / OpenCodeAdapter / GeminiAdapter), extract `tm/llm/_subprocess_adapter.py` with shared `_resolve_binary`, `_serialize_messages`, `_run_subprocess` helpers. Currently CodexAdapter + ClaudeCodeAdapter duplicate ~150 lines of subprocess scaffolding; with 2 adapters this is fine, with 4+ it becomes a maintenance liability. Final-review carry-forward from the multi-backend wave (`6755760`).
- **`warnings.warn` on stub `tool_call` invocation** in CodexAdapter + ClaudeCodeAdapter. Currently both silently return `tool_calls=[]`; one-time warning would catch surprised callers in v1.1 when `tm.agents.*` may grow new tool-using paths.
- OllamaAdapter / OpenAIAdapter / OpenCodeAdapter / GeminiAdapter (additional LLM backends — factory shape is ready for them; do the shared-helper extraction above FIRST)
- CalDAV/calendar sync, Telegram bot polling/webhooks, voice transcription, pattern reconciliation
- Pydantic v2 conversion once anthropic's transitive pin is project-stable
- Real eval set (≥30 real + ≤20 synthetic) in companion repo `time-manager-evals`

---

## Build statistics

- **Subtasks shipped:** ~53 (across 19 dispatch waves)
- **Commits:** ~135
- **Tests:** 711 passed + 1 skipped + 0 failed
- **Schema migrations:** 11
- **Source files:** 58 (mypy strict-clean)
- **Total Python files (incl. tests):** 117 (ruff + format clean)
- **Cumulative orchestrator + worker + reviewer tokens:** ~3.4M of 4M daily cap (~85%)

---

## v1 signoff

**Granted (caveats closed).** The four headline deliverables (process mining, vocabulary governance, first-class goals, outcome scoring) are production-grade and ship as a coherent integrated product. End-to-end pipeline is now reachable from the CLI: `tm init → goal add → debrief transcript.txt → discover → suggest`. All three originally critical caveats are closed. The audit-surfaced operator-surface gap (no `tm debrief` / `tm suggest` CLI) is also closed. Tier A (polish carry-forward), Tier B (discrepancies), Tier M (real follow-ups), and Tier P (review-driven cleanup) all shipped.

**Production-readiness:** the system is shippable for a first non-developer user. Open items are Tier S (strategic / scope decisions) and v1.1+ backlog.

This document supersedes worker return summaries at the v1 level; the four area release docs supersede them at the area level.
